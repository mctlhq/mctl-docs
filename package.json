{
  "name": "mctl-docs",
  "version": "0.1.29",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vitepress dev docs",
    "generate:llms": "bun run scripts/generate-llms-full.ts",
    "build": "bun run generate:llms && vitepress build docs",
    "preview": "vitepress preview docs"
  },
  "devDependencies": {
    "@mctlhq/css": "^0.3.0",
    "mermaid": "^11.13.0",
    "vitepress": "^1.6.0"
  }
}
